import React from "react";
import ReactDOM from "react-dom/client";
import { VSCodeOfficeStudio } from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <VSCodeOfficeStudio />
  </React.StrictMode>,
);
